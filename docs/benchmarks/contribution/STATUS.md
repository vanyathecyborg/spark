# Contribution gates

- Packaging: draft [PR #421](https://github.com/sparkjsdev/spark/pull/421), commit 647e3d6. Local packed consumer tests pass Node 22.22.0/23.10.0, Three.js 0.180.0/0.185.1. Linux/Windows CI requires first-contributor approval from an upstream maintainer.
- Ordering allocation: branch codex/webgl-ordering-buffers, commit 82d463a. Actual buffer transfer, alias/reuse and GPU ordering-restoration checks pass. The full renderer has a preexisting context-restoration image defect, reproduced on unchanged upstream. Timing qualification remains open.
- Exact-order traversal: branch codex/lod-traversal, head a8de968 (algorithm/test commit 8adc6be). Native and WASM differential tests pass exact ordered output. Pooled output storage has approximately neutral latency. The faster early-terminal experiment changed equal-depth blending and is excluded.
- Native reference: development checkpoint b1e45eb in vanyathecyborg/sparkjsv2-webgpu, not an upstream PR base. Actual r180/source and r185/packed-package pixels, transparency, tone mapping, occlusion, resize and explicit unsupported-feature checks pass. Radix has 40 actual GPU comparisons. Full feature/lifecycle/generation validation and contribution extraction remain open.
- Timing: both ordering cohorts retain all runs but have performanceClaimQualified=false. Other GPU activity and substantial run variance prevent a renderer-speedup claim. No unfavorable valid run was discarded.
- Publication: the scanned houseplant is attributed under CC BY 4.0; synthetic scaling is original MIT material. Final controlled timing matrix, instrumentation overhead, two separately recorded normal-speed videos and performance-based social drafts remain pending. No merge, maintainer endorsement or cross-platform hardware-performance result is implied.
